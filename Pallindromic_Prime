# To Check Prime And Pallindorme Number 

num = int(input("Hey Please Enter a Number : "))

reverse = int(str(num)[::-1])

if num == reverse:

  if num > 1:
    for i in range(2,num):
      if num % i == 0:
        print(f"{num} is Not Prime But it is Pallindrome")
        break
    else:
      print(f"{num} is Prime and Paliindorme")

else:
  if num > 1:
    for i in range(2,num):
      if num % i == 0:
        print(f"{num} is Neither Prime Nor Pallindrome")
        break
    else:
      print(f"{num} is Prime But Not Pallinrome")
